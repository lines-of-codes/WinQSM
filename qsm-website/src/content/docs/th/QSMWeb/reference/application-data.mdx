---
title: ข้อมูลแอพลิเคชัน
description: สถานที่ที่ QSM Web เก็บข้อมูล
---

import { FileTree } from "@astrojs/starlight/components";

QSM Web เก็บข้อมูลไว้ที่สถานที่ต่อไปนี้บนระบบปฏิบัติการต่าง ๆ

- Linux: `/var/lib/qsm-web`
- FreeBSD: `/usr/local/etc/qsm-web/`
- Windows: `C:\ProgramData\QSMWeb\`
- macOS: โฟลเดอร์ที่แอพลิเคชันตั้งอยู่

## โครงสร้างโฟลเดอร์

คล้ายธรรมชาติของ QSM Windows, QSM Web มีโครงสร้างระบบไฟล์ดังนี้

<FileTree>
    
    - config.json การตั้งค่าแอพลิเคชัน
    - downloads/
    - logs/
    - qbs/ สถานที่ติดตั้ง QBS
    - servers
        - [server_name]
            - config.qsm.json การตั้งค่าเฉพาะเซิร์ฟเวอร์ของ QSM
            - ...
    
</FileTree>

## เปลี่ยนสถานที่ตั้งข้อมูล

คุณสามารถเปลี่ยนโฟลเดอร์ข้อมูลได้โดยการแก้ไข `appsettings.json` และเพิ่มคีย์การตั้งค่า `QSM:DataFolder` ดังนี้:

```json
  "QSM": {
    "DataFolder": "/abc/def"
  }
```

หรือคุณก็สามารถใช้ตัวแปรสิ่งแวดล้อม `QSM__DataFolder` ได้เช่นกัน
